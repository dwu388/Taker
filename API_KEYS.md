# API credentials

Copy `.env.example` to `.env` and populate only if/when you intentionally use provider commands.

```text
HELIUS_API_KEY=
SHYFT_API_KEY=
BIRDEYE_API_KEY=
```

`.env` is excluded by `.gitignore`. Do not hard-code credentials into Python or batch files.
